label('Once a debt has reached the end of its statue of limitations is should be removed from the consumer credit report.', a).
label('However, collectors use the law to refresh old debts so they effectively never leave a report.', b).
label('If a collector can\'t sue to retrieve the debt it has no business on a consumer report.', c).
type(a, policy).
type(b, fact).
type(c, value).
