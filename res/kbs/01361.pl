label('They should not have free-to-end-user', a).
label('it will just making worse for consumer.', b).
label('This does not make sense.', c).
type(a, policy).
type(b, value).
type(c, value).
link(b, a, reason).
