label('Why are these questions putting the onus, once again, one the consumer?', a).
label('It is the collectors who have harassed in the name of collecting a debt.', b).
label('It is pretty easy to distinguish landline from cellphone', c).
label('google the number, that tells you.', d).
label('But creditors use recorded calling,', e).
label('that should be stopped.', f).
label('If they have a well-trained person, that person ought to be able to determine the time zone the consumer lives in.', g).
label('I agree with the previous comment.', h).
label('Creditors should assume the current mailing address is the correct one.', i).
label('How difficult is it to go from there in assessing the times that are appropriate to make calls?', j).
type(a, value).
type(b, value).
type(c, value).
type(d, fact).
type(e, fact).
type(f, policy).
type(g, value).
type(h, value).
type(i, policy).
type(j, value).
link(b, a, reason).
link(d, c, reason).
link(i, h, reason).
